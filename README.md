# homebrew-nodex

First-party Homebrew tap for Nodex.

The `Casks/nodex.rb` file is published automatically by the Nodex release workflow after a tagged macOS release completes successfully.

Install after the first cask publication:

```bash
brew tap Asphocarp/nodex
brew install --cask nodex
```
